@model AffiliateTypeModel
@{
    //page title
    ViewBag.Title = T("Admin.AffiliateTypes.AddNew").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("AffiliateTypes");
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Admin.AffiliateTypes.AddNew")
            <small><i class="fa fa-arrow-circle-left"></i>@Html.ActionLink(T("Admin.AffiliateTypes.BackToList").Text, "TypeList")</small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.SaveContinue")
            </button>
        </div>
    </div>
    @Html.Partial("_TypeCreateOrUpdate", Model)
}